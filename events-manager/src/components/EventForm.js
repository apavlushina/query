// Create the EventForm container.
// This should be a simple functional component
// (without state) calling the onSubmit and
// onChange callbacks. The <input> fields should
//  also use the values. Add a field for name,
// date and description and a submit button.
